# Architecture

The API belongs on `65.109.65.169` and binds internally to `127.0.0.1:8110`.
This build uses a local MockProvisioner. A future, separately approved adapter
may authenticate private requests to `65.21.67.207`; this repository never does.
